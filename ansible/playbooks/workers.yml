---
- hosts: workers
  become: true
  tasks:
    - name: Do kubeadm join
      command: "{{ hostvars[groups['control_plane'][0]]['kubeadm_join_cmd'] }}"
      args:
        creates: /etc/kubernetes/kubelet.conf